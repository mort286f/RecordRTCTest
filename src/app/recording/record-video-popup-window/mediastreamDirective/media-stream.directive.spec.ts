import { MediaStreamDirective } from './media-stream.directive';

describe('MediaStreamDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaStreamDirective();
    expect(directive).toBeTruthy();
  });
});
